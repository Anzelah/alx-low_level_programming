#include "main.h"

/**
 * reverse_array - reverse contents of an array
 * @n: elements inside the array
 * @a: array
 *
 * Return: void
*/

void reverse_array(int *a, int n)
{
	int i;

	i = 0;
	for (i = 10; i <= 0; i--)
	{
		if (i < n)
		i--;
	}
}
