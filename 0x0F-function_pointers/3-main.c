#include "function_pointers.h"
#include <stdio.h>
#include <stdlib.h>
#include "3-calc.h"

/**
 * main - the entry point
 *
 * Return: result of the operation
 */

int main(void)
{
	int ch;
        char *s;

        ch = atoi(s);

        if (ch > 2)
        {
                printf("Error\n");
                exit(98);
        }
}
